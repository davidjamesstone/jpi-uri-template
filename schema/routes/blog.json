{
  "type": "object",
  "title": "Customer API",
  "description": "Customer API description",
  "definitions": {
    "id": {
      "type": "string",
      "description": "Product SKU",
      "example": "ABC-123"
    },
    "customerId": {
      "type": "string",
      "description": "Product SKU",
      "example": "ABC-123"
    }
  },
  "links": [
    {
      "title": "Create",
      "description": "Create a new customer.",
      "href": "/customer",
      "method": "POST",
      "rel": "create",
      "schema": {
        "type": "object",
        "properties": {
          "name": { "$ref": "#/properties/name" },
          "age": { "$ref": "#/properties/age" }
        }
      },
      "targetSchema": {
        "rel": "self"
      },
      "required": ["name", "age"]
    },
    {
      "description": "Blog post info for existing customer.",
      "href": "/customer/{#/definitions/id}/{#/definitions/customerId}/{age}?orderBy={orderBy}",
      "method": "GET",
      "rel": "self",
      "title": "Info",
      "targetSchema": {
        "rel": "self"
      }
    }
  ],
  "properties": {
    "id": { "$ref": "../common/id.json" },
    "name": {
      "type": "string",
      "example": "Customer name"
    },
    "age": {
      "type": "integer",
      "minimum": 0,
      "example": 27
    },
    "orderBy": {
      "type": "string",
      "enum": ["price", "distance", "postage"],
      "title": "Order by clause",
      "description": "Order by",
      "example": "price"
    },
    "shipTo": {
      "$ref": "../common/address.json"
    },
    "billTo": {
      "$ref": "../common/address.json"
    }
  },
  "required": ["id", "name", "age", "shipTo", "billTo"],
  "additionalProperties": false
}